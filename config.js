/* Recoup runtime config — externalized so pages need no inline <script>, enabling a strict CSP (no 'unsafe-inline'). */
window.RO_CONFIG = { apiBase: "https://vaibhav3313-recoup.hf.space" }; // live backend on Hugging Face Spaces
